version: "3.4"
services:
  consensus:
    command:
      - ${LH_RAPID_SYNC}
      - --staking
      - --subscribe-all-subnets
      - --import-all-attestations
      - --logfile-max-number=0
      - --enr-udp-port=${CC_P2P_PORT:-9000}
      - --network-load=5
